
[ ] replace Test Scene dropdown control with 4 button switcher with expressive icons
[ ] For flattened mode of the text let's add increase decrease font size controls as optional extension, controlled by the toggle in Lab panel.  I'm experimenting, trying to figure out if we can make the text line more useable
[x] for text editing mode, for the Grouped behavior let's use the style of the toolbar from the Flat, when text controls are explicit one line toolbar - popover for the font steals too much space. Make that behavior controlable from the Lab panel and enabled by default
[ ] propose a good, reasonable and easy to learn shortcut schema. Where possible keep it consistent with the most popular tools in the industry 
[ ] add a 3-vertical-dot menu at the end of formatting toolbar. It will be used for rare operations/actions such as Set default style, copy/paste style etc. Make it optional via toggle in the Lab panel.
[ ] when border color selected as transparent, but user clicks onto one of the style controls - automatically select dark color, so there will be some visible reaction and people will not need to build a mental path - "select color first to show the border"
[ ] lets' make trasperent color circles for Stroke looks as no-line, rather than just transparent - could be an crossed red circle with transparent background
